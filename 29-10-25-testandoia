#include <stdio.h>

int main() {
    int vetor[] = {3, -2, 8, 0, -5, 12};
    int tamanho = 6;
    
    int maior = maior_elemento(vetor, tamanho);
    printf("Maior elemento: %d\n", maior); // Output: 12
    
    return 0;
}